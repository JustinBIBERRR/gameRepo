# 看图猜梗 — 待澄清清单

以下为提案与规格中尚未完全确定或存在多种理解的点，可按编号确认选项（或回复「由实现决定」）。确认后我会同步更新 proposal / spec / tasks。

**已确认（1–19）**：1 A，2 B，3 B，4 C，5 A，6 A，7 A，8 A，9 A，10 A；11 A，12 A，13 A，14 B（默认 2 条示例：pigGuess.png→二锅头，roubaoGuess.png→书包），15 A，16 A，17 A，18 A，19 A。已全部写入 proposal / spec / tasks。

---

## 一、题目与选题

| # | 问题 | 选项 / 建议 |
|---|------|-------------|
| **1** | **选题方式**：从「尚未使用」的题目中选一道时，用**随机**还是**按配置顺序**？ | A. 随机（推荐：每次体验不同）<br>B. 严格按配置顺序（便于主持人控场）<br>C. 由实现决定 |
| **2** | **题目只有 1 道时**：猜对后「再来一局」必然又是同一道。是否需要在 UI 上提示「当前题库仅 1 道题」或接受默认行为即可？ | A. 接受默认行为，不特别提示<br>B. 当题库只有 1 道时，在页面或成功提示中说明「题库仅 1 题」<br>C. 由实现决定 |

---

## 二、倒计时与游戏参数

| # | 问题 | 选项 / 建议 |
|---|------|-------------|
| **3** | **是否支持倒计时**：tasks 中写「按需支持倒计时」，当前 spec 未写。 | A. 不支持倒计时，保持极简（推荐：与「不限制次数」一致）<br>B. 支持可选倒计时（与电影等游戏一致，可配置每题/每局时长）<br>C. 由实现决定 |

---

## 三、图片与数据

| # | 问题 | 选项 / 建议 |
|---|------|-------------|
| **4** | **图片存储方式**：Schema 写「image 可为 URL 或文件引用」。 | A. 仅支持**在线 URL**（实现简单，无存储压力）<br>B. 仅支持**本地上传**（File → IndexedDB，与听歌/电影一致，离线可用）<br>C. **两者都支持**：可填 URL，也可上传本地文件（推荐：灵活）<br>D. 由实现决定 |
| **5** | **图片大小/格式限制**：若支持本地上传，是否限制格式（如仅 jpg/png/gif）和单张大小（如 ≤2MB）？ | A. 不限制（由实现决定合理上限）<br>B. 限制格式 + 单张上限（如 2MB），与现有游戏风格一致<br>C. 由实现决定 |
| **6** | **某题图片加载失败时**（URL 失效、文件损坏等）： | A. 自动**跳过该题**，选下一道未使用题；若全部失败则提示「暂无可用题目」<br>B. 仍展示该题，显示占位图 + 错误提示（如「图片加载失败」），允许继续猜<br>C. 由实现决定 |

---

## 四、「已使用」记忆与重置

| # | 问题 | 选项 / 建议 |
|---|------|-------------|
| **7** | **「已使用题目」记忆存在哪**：影响刷新/关标签后的行为。 | A. **仅内存**：刷新页面即清空，关标签必清（推荐：与「重置记忆」试玩场景一致）<br>B. **sessionStorage**：刷新保留、关标签清；重置记忆按钮清 sessionStorage<br>C. 由实现决定 |
| **8** | **「重置记忆」按钮位置**： | A. 游戏页内**页头区域**（如与标题同一行右侧，显眼但非主操作）<br>B. **成功提示区域**下方（猜对后与「再来一局」并列）<br>C. 由实现决定 |

---

## 五、答案与匹配

| # | 问题 | 选项 / 建议 |
|---|------|-------------|
| **9** | **猜对后是否在成功页展示答案**：如「答案是：xxx」。 | A. **展示**（与电影/城市等一致，推荐）<br>B. 不展示，仅提示「猜对了」<br>C. 由实现决定 |
| **10** | **答案匹配规则**：是否与电影猜测**完全复用**同一套（精确 → 别名 → 模糊，忽略大小写/空格/标点），包括复用 `pinyinUtils` 等？ | A. 是，复用现有匹配工具与规则（推荐：一致、少维护）<br>B. 仅基础模糊（trim + 忽略大小写/标点），不做拼音等<br>C. 由实现决定 |

---

## 六、游戏统计与失败态

| # | 问题 | 选项 / 建议 |
|---|------|-------------|
| **11** | **是否展示本局/本次会话统计**：如「已猜对 3 题」「失败 1 题」（仅内存，刷新重置）。 | A. **不展示**，保持界面极简（推荐）<br>B. 展示简单统计（与 platform-core 的「游戏统计」一致）<br>C. 由实现决定 |
| **12** | **次数用尽（判负）后的入口**：当前 spec 写「展示再来一局或下一题」。 | A. 统一为**「再来一局」**按钮（与猜对后一致，推荐）<br>B. 区分「下一题」与「再来一局」文案<br>C. 由实现决定 |

---

## 七、设置与数据策略

| # | 问题 | 选项 / 建议 |
|---|------|-------------|
| **13** | **题目条数上限**：是否与现有游戏一致（如 500 条）？ | A. 是，500 条上限，与 settings-admin 一致（推荐）<br>B. 单独设上限（请注明）<br>C. 由实现决定 |
| **14** | **默认题目数据**：首次进入、未做任何配置时。 | A. **0 条**，引导去设置页配置（与部分游戏一致）<br>B. **3–5 条示例**梗题（占位图 + 示例答案），开箱可玩<br>C. 由实现决定 |
| **15** | **数据过期**：看图猜梗的「自定义数据」是否参与设置里的**数据过期清除**（过期后恢复默认）？ | A. 是，与其他游戏一致（推荐）<br>B. 不参与<br>C. 由实现决定 |

---

## 八、国际化与路由

| # | 问题 | 选项 / 建议 |
|---|------|-------------|
| **16** | **i18n**：是否需要英文文案（en.json）？ | A. 是，与现有游戏一致，中英键都补（推荐）<br>B. 仅中文（zh-CN.json）<br>C. 由实现决定 |
| **17** | **路由路径**：是否确定为 `/image-meme-guess`？ | A. 是<br>B. 其他（请注明，如 `/meme-guess`） |

---

## 九、配置变更与边界

| # | 问题 | 选项 / 建议 |
|---|------|-------------|
| **18** | **配置变更生效时机**：管理员在设置中改了题目列表或最大尝试次数后。 | A. 与 settings-admin 一致：**进行中不生效**，下一局或重新进入游戏后生效（推荐）<br>B. 需在 spec 中显式写一条 Scenario<br>C. 由实现决定 |
| **19** | **管理员删除了当前正在玩的某题**：玩家正在猜的题目在设置里被删掉。 | A. 与现有游戏一致：进行中不受影响，可正常猜完；新一局不再出现该题（推荐）<br>B. 需在 spec 中显式写一条<br>C. 由实现决定 |

---

请直接回复编号 + 选项（如「1 A，3 A，4 C，7 A…」），或对某几条写「由实现决定」。我会根据你的回复更新 proposal / spec / tasks，并标出已澄清与仍由实现决定的部分。
