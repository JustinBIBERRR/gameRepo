## 1. Implementation

- [x] 1.1 在 `src/config/games.ts` 中新增游戏类型与配置（如 `imageMeme`），在 `src/router/index.ts`、`src/views/Home.vue`、`src/views/Settings.vue` 中注册看图猜梗的路由、首页卡片与设置入口。
- [x] 1.2 定义看图猜梗数据 Schema（每题：图片 + 答案，无提示字段），在 `src/schemas/` 中新增并在设置中绑定。
- [x] 1.3 新建 `ImageMemeSettings.vue`，提供游戏参数：默认不限制尝试次数，可选配置「最大尝试次数」与**可选倒计时**（与电影等一致）；数据管理（图片支持 URL + 本地上传，格式/大小由实现定合理上限；CRUD、默认/自定义、导入导出）；不提供「提示」相关开关。
- [x] 1.4 新建看图猜梗游戏页（如 `ImageMemeGuess.vue`）：题目**随机**从「尚未使用」的题中选取，用光后自动重置记忆再选；**已使用记忆仅存内存**，刷新即清；某题**图片加载失败则跳过**该题；输入框纯文本、**无联想/下拉**；答案匹配**复用**现有工具（与电影一致）；猜对后**展示答案**并提供「再来一局」；**页头**提供「重置记忆」按钮；题库仅 1 题时提示「当前题库仅 1 题」。
- [x] 1.5 实现题目与答案的存储与读取（localStorage/IndexedDB 与现有游戏一致）；答案匹配**复用**现有工具（如电影猜测的匹配逻辑）；图片支持 URL 与本地上传（本地上传可存 IndexedDB）。
- [x] 1.6 提供**默认 2 条题目**：`img/guessPic/pigGuess.png` → 二锅头，`img/guessPic/roubaoGuess.png` → 书包；补充**中英 i18n**（`games.imageMeme.title` 等），确保默认即可体验。

## 2. Validation

- [ ] 2.1 默认 2 条题目（pigGuess→二锅头、roubaoGuess→书包）开箱可玩；在设置中配置多道题目（图片 + 答案），确认保存、列表、编辑、删除与导入导出正常，500 条上限生效。
- [ ] 2.2 进入游戏后确认只显示当前题目图片，输入框无联想下拉；输入正确答案后判定成功并出现「再来一局」。
- [ ] 2.3 答案匹配：验证精确、别名、模糊（大小写/空格/标点忽略）行为符合预期。
- [x] 2.4 运行 `npm run build` 确认类型检查与构建通过。
