## 1. Schema 与存储
- [x] 1.1 更新听歌识曲 Schema：新增 `artist`（必填）；将播放用音频字段语义改为「AI 生成歌（MP3）」并设为必填；移除 `lang`、`rate`、`pitch`；保留 `lyrics`、`answer`、`hints`。
- [x] 1.2 更新 `ListenSongItem` 与存储：增加 `artist`；播放用音频仍存为 Blob（可沿用 `audioClip` 键名），校验必填与 5MB 限制。
- [x] 1.3 默认/示例数据：提供**一条**默认示例——方大同《特别的人》；歌词为「懂一個人也許要忍耐 要經過了意外 才瞭解所謂的愛 今後的歲月 讓我們一起瞭解 多少天長地久 有幾回細水長流」，答案「特别的人」，歌手「方大同」；音频使用 bundled 资源 `src/data/specialPerson.mp3`。「导入示例数据」时 fetch 该 MP3 转为 Blob/File 后与元数据一并写入 IndexedDB。

## 2. 设置页
- [x] 2.1 听歌识曲数据管理：表单与列表展示 `artist`、播放用音频（必填）；移除语种/语速/音调表单项；文案与校验与 Schema 一致。

## 3. 游戏页
- [x] 3.1 播放逻辑：用 `<audio>` 播放当前题的 MP3（从 IndexedDB 取 Blob 生成 URL），替换 TTS 朗读；提供「播放」「重播」；无音频时禁用播放并提示。
- [x] 3.2 揭晓：揭晓时仅展示歌词文本与歌手信息，移除揭晓音频播放与相关 UI。
- [x] 3.3 选题与校验：仅加载含有效播放用音频的题目；`getSongsForGame` / 选题逻辑与数据加载校验与新区据模型一致。

## 4. 文案与验收
- [x] 4.1 更新听歌识曲相关 i18n（描述、按钮、提示等）。
- [x] 4.2 手动验收：配置一条「MP3 + 歌词 + 歌手」数据，完整走通播放→猜题→揭晓（仅文本）。
