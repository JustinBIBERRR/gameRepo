/**
 * 游戏组件注册
 * 
 * 此文件用于未来注册全局组件
 * 目前项目使用自定义组件，不依赖第三方组件库
 */

import type { App } from 'vue'

/**
 * 安装函数 - 用于注册全局组件
 * 目前为空实现，后续如需注册全局组件可在此添加
 */
export function install(_app: App): void {
  // 未来可在此注册全局组件
}

export default { install }
